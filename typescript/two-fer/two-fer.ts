/**
 * This stub is provided to make it straightforward to get started.
 */

export function twoFer(name:string|null = 'X'): string {
    const value = name === 'X' ? 'you' : name
  return `One for ${value}, one for me.`;
}

